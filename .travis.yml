language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

before_install:
  - pip install --user cpp-coveralls

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - ctest

after_success:
  - coveralls --root ..

notifications:
  email: false
